# Migrations

As migrations são geradas automaticamente pelo Prisma CLI:

```bash
npx prisma migrate dev --name <nome-da-migration>
```

Os arquivos gerados nesta pasta representam o histórico de alterações do banco de dados.
