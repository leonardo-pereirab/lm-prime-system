export async function gerarPdfContrato(
  textoContrato: string,
  nomeArquivo: string,
): Promise<Buffer> {
  void textoContrato;
  void nomeArquivo;
  throw new Error("gerarPdfContrato: implementacao pendente");
}
